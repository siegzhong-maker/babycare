I will transform the current single-page prototype into a **complete, multi-view Mini Program simulation** that fully implements the V2.0 PRD requirements.

This "Whole Project" iteration will involve restructuring the codebase to support the full user flow (Home -> Chat), enabling the actual "App-like" experience described in the PRD.

### 1. Architecture Refactoring (Multi-View Logic)
*   **Implement a SPA (Single Page App) Router**: Create a lightweight view manager to switch between:
    *   **Home Page (Landing)**: The "4 Palace Grid" (Eat, Sleep, Poop, Grow) as the main entry point.
    *   **Chat Page**: The conversation interface, initialized with context based on the Home selection.
    *   **Profile Page**: User settings and baby archives.
*   **State Management**: Centralize `babyProfile`, `chatHistory`, and `currentContext` to share data across views.

### 2. Full Feature Implementation (Per PRD)
*   **Home Grid (The 4 KEPs)**:
    *   Implement the 4 core entry points: **[喂养] (Eat)**, **[睡眠] (Sleep)**, **[排泄] (Health)**, **[护理] (Care)**.
    *   *Logic*: Clicking a grid item (e.g., "Sleep") immediately navigates to Chat and sends a context prompt (e.g., "Aunt Li, I have a sleep question...").
*   **AI Persona & Logic (The Brain)**:
    *   **Persona**: Enforce "**李阿姨 (Aunt Li)**" (30y exp, warm, colloquial).
    *   **Triage System**: Refine the System Prompt to **mandate** 1-2 clarifying questions for vague inputs before giving advice.
    *   **SOP Wizard**: Full implementation of the JSON-based Instruction Cards (Preparation -> Steps -> Completion).
    *   **Risk Control**: Strict "See a Doctor" trigger for fever/injury.
*   **UI/UX Overhaul**:
    *   **Visuals**: Update to a warm, trustworthy color palette (Orange/Green).
    *   **Interaction**: Ensure all buttons and cards are thumb-friendly (single-hand operation).

### 3. Tech & Configuration
*   **Model**: Switch to `google/gemini-2.0-flash-lite-preview-02-05:free`.
*   **Persistence**: Save Baby Profile and History to `localStorage` so data survives refreshes.

I will implement this as a robust `index.html` (updating `proto1.html`) that functions as the complete, demonstrable MVP of the Mini Program.