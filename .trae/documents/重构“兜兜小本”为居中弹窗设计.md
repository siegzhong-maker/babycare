我将根据您提供的设计图，将“兜兜小本”从**底部抽屉**改为**居中弹窗（Dialog）**样式，并重新设计内部表单布局。

### 1. 布局重构 (HTML/CSS)
*   **容器样式**：
    *   将弹窗位置调整为**屏幕居中**。
    *   内容容器改为**圆角卡片**（`rounded-2xl`），宽度适配屏幕但留有边距。
*   **表单风格调整**（完全匹配设计图）：
    *   **标签位置**：从“左侧对齐”改为**“上方对齐”**。
    *   **输入框**：改为**灰色背景**（`bg-gray-50`）的圆角矩形，移除底部边框线。
    *   **性别选择**：改为两个**并排的大按钮**。选中状态为“绿框淡绿背景”，未选中为“灰框白背景”。
    *   **状态区域**：将 4 个状态指标（喂奶、尿布、体温、睡眠）改为 **2x2 网格布局**，每个格子包含标签和输入框。
*   **按钮优化**：
    *   “保存档案”按钮改为底部通栏的大圆角绿色按钮。

### 2. 动画效果更新 (CSS/JS)
*   移除之前的“从下划入”动画。
*   新增**“缩放淡入”**（Scale Fade In）动画，使弹窗在打开时呈现轻微的放大浮现效果，更符合居中弹窗的交互逻辑。

### 3. 代码修改范围
*   `proto.html`: 修改 CSS 样式定义、Modal 的 HTML 结构以及相关的 JS 交互逻辑。
