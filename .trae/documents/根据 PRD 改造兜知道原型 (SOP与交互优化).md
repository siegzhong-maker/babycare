我将根据 PRD 文档对 `proto1.html` 进行以下改造：

1. **优化快捷追问胶囊 (Suggestions) 交互**：

   * **现状**：目前 AI 的建议追问是渲染在聊天记录流中的。

   * **改造**：根据 PRD “位于输入框上方”的要求，我将修改 JS 逻辑，使得 AI 返回的 `suggestions` 动态更新到底部固定的 `#suggestion-bar` 区域。这样用户在阅读完回复后，可以直接在输入框上方点击追问，交互更流畅。

2. **强化 SOP 向导卡片样式**：

   * **现状**：已有基础结构，但样式细节需对齐 PRD。

   * **改造**：

     * 微调“阶段 B：分步执行”的 UI，确保“灰色底色块”描述醒目且易读。

     * 优化底部“上一步/下一步”按钮的布局比例。

     * 确保“阶段 A”的圆形选择框交互符合“变实心绿”的视觉反馈。

3. **更新 System Prompt (AI 人设)**：

   * **改造**：在 `getSystemPrompt` 中明确注入“Wizard 模式”指令，要求 AI 在遇到复杂操作（如洗澡、抚触）时，严格返回符合三段式结构（准备-执行-完成）的 JSON 数据，并保持“兜兜阿姨”的亲切口吻。

4. **首页宫格与默认内容微调**：

   * 检查并微调首页 2x2 宫格的图标和文案。

   * 更新底部默认的快捷胶囊内容，使其更符合“用户视角提问”的规范（如“宝宝哭了怎么办？”）。

**修改文件**：

* `/Users/silas/Desktop/baby care/proto1.html`

