好的，我将优化 SOP 引导卡片的交互，**移除准备阶段的“勾选”操作**，改为**纯展示的序号列表**，让交互更加轻量直接，符合“阅后即开始”的逻辑。

### 修改计划

1.  **组件结构 (`sop-wizard.wxml` & `proto1.html`)**:
    *   **移除 Checkbox**: 删掉 `prep-check` 勾选框及点击事件。
    *   **新增序号展示**: 在准备项前添加 `01`, `02` 样式的数字序号，强调步骤感。
    *   **文案优化**: 标题栏直接展示“准备清单 (共N项)”。

2.  **组件样式 (`sop-wizard.wxss`)**:
    *   设计新的序号样式：使用等宽字体或小的灰色色块包裹数字，保持 Z 世代的极简工业风。

3.  **逻辑清理 (`sop-wizard.js`)**:
    *   移除 `checkedItems` 状态管理和 `togglePrep` 点击事件，简化代码。

我将立即执行这些变更。