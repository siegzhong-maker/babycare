# Fix Missing SOP Step Content

The user reported that SOP steps are empty. The issue likely stems from the system prompt in `miniprogram/pages/index/index.js` not explicitly defining the structure of the `steps` array objects (specifically requiring `title` and `desc` fields).

## 1. Update System Prompt (`miniprogram/pages/index/index.js`)
I will modify the `systemPrompt` variable in `sendMessage` function to be more explicit about the JSON structure, specifically the `steps` array.

**Current:**
```javascript
"sopData": { "title": "...", "preps": [...], "steps": [...] },
```

**New:**
```javascript
"sopData": { 
  "title": "...", 
  "preps": ["准备1", "准备2"], 
  "steps": [
    {"title": "步骤1标题", "desc": "步骤1详细操作说明"},
    {"title": "步骤2标题", "desc": "步骤2详细操作说明"}
  ] 
},
```

## 2. Verify `sop-wizard` Component
I've already checked `sop-wizard.wxml` and it correctly binds to `data.steps[currentStep].desc`. The issue is almost certainly upstream in the AI generation.

## Plan Steps
1.  **Modify `index.js`**: Update the `systemPrompt` to include a detailed schema example for `sopData.steps`.
