# Optimize Recommendation Area

I will update the recommendation area to show 3 random high-frequency questions with an improved visual style.

## 1. Logic Update (`miniprogram/pages/index/index.js`)
- Change `getRandomSuggestions(5)` to `getRandomSuggestions(3)` to limit the number of recommendations to 3.
- Ensure the `HIGH_FREQ_QUESTIONS` list is used as the source.

## 2. UI Update (`miniprogram/pages/index/index.wxml`)
- Remove the horizontal `scroll-view` structure for recommendations.
- Replace it with a vertical list or a clean flex-row layout that fits 3 items nicely. given the mobile context and "concise" requirement, a vertical stack of "cards" or "bubbles" might take too much space, while a horizontal row might cramp text.
- **Decision**: Use a **vertical list of small, clickable bubbles/chips** located above the input bar, but designed to look like a "Quick Ask" area. Or better, keep the horizontal layout but ensure it's not a scroll view if they fit, OR keep the scroll view but style the chips to be more attractive (e.g., soft background colors, icons).
- **Refined UI**: I will keep the `scroll-view` but optimize the `.chip` style to be more "Morandi" and attractive. I'll add a small "💡 猜你想问" (Guess you want to ask) label above the chips to distinguish the area.

## 3. Style Update (`miniprogram/pages/index/index.wxss`)
- Update `.chip` styles:
    - Add a soft background color (`var(--brand-primary-light)`).
    - Add a subtle border and shadow.
    - Improve font size and padding.
- Add styles for the new "Guess you want to ask" label.

## Plan Steps
1.  **Modify `index.js`**: Update `onLoad` to fetch 3 suggestions.
2.  **Modify `index.wxml`**: Add a title/label for the suggestion area and adjust the layout structure if needed.
3.  **Modify `index.wxss`**: Refine the visual styles for the suggestion chips and the new label.
